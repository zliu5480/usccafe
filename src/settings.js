module.exports = {
    // title
    title: "USC Covid Admin System",
    // default connection time out in ms (600000ms = 1 minute)
    timeout: 600000,
    //show tag or not
    tagsView: true,
    //fix header or not
    fixedHeader: true,
    // time for token to be stored in cookie in days
    tokenCookieExpires: 1,
    // time for token to be stored in cookie in days
    passCookieExpires: 1,
    // allow only one submenu
    uniqueOpened: true,
    TokenKey: 'TOKEN',
    // show logo or not
    sidebarLogo: true,
    // show bottom info or not
    showFooter: true,
    // bottom info
    footerTxt: '',
    // case number
    caseNumber: ''
}
